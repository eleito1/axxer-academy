<x-app-layout title="Administração — AXXER Academy"><h1>Visão geral</h1><p>Acompanhe a operação e o consumo dos treinamentos.</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:12px;margin:24px 0">@foreach(['Produtos'=>$stats['products'],'Cursos'=>$stats['courses'],'Módulos'=>$stats['modules'],'Aulas'=>$stats['lessons'],'Alunos'=>$stats['students'],'Pendentes'=>$stats['pending'],'Cursos publicados'=>$stats['published_courses']] as $label=>$value)<div class="card" style="padding:18px"><small class="muted">{{ strtoupper($label) }}</small><strong style="display:block;font-size:27px;margin-top:7px">{{ $value }}</strong></div>@endforeach</div>
<div class="grid"><section class="card"><h2>Cursos mais assistidos</h2>@forelse($topCourses as $course)<div style="margin:15px 0"><div class="actions" style="justify-content:space-between"><strong>{{ $course->title }}</strong><small>{{ $course->accesses }} acessos</small></div><div style="height:7px;background:#edf0f5;border-radius:10px;margin-top:6px"><div style="height:100%;width:{{ $course->chart_percentage }}%;background:#1559e8;border-radius:10px"></div></div></div>@empty<p>Sem acessos registrados.</p>@endforelse</section><section class="card"><h2>Atividade</h2><p><strong style="font-size:27px">{{ $activeStudents }}</strong><br><span class="muted">alunos ativos nos últimos 30 dias</span></p><p><strong style="font-size:27px">{{ $stats['completed_lessons'] }}</strong><br><span class="muted">aulas concluídas</span></p></section></div>
<section class="card" style="margin-top:20px"><h2>Últimos acessos</h2>@forelse($latestAccesses as $access)<div class="user"><strong>{{ $access->user->name }}</strong><span class="muted"> abriu “{{ $access->lesson->title }}” · {{ $access->updated_at->diffForHumans() }}</span></div>@empty<p>Nenhum acesso registrado.</p>@endforelse</section>
<section class="card" style="margin-top:20px"><h2>Administração de usuários</h2><p>Aprove, bloqueie e defina os produtos disponíveis para cada aluno.</p>@foreach($users as $user)<article class="user"><div><strong>{{ $user->name }}</strong> <span class="badge {{ $user->status->value }}">{{ strtoupper($user->status->value) }}</span><div class="muted">{{ $user->email }} · {{ $user->company }} · {{ $user->city }} · {{ $user->whatsapp }}</div><div class="muted">Interesse: {{ $user->interestedProduct?->name ?? 'Não informado' }}</div></div><div class="actions" style="margin-top:14px"><form method="POST" action="{{ route('admin.users.status',$user) }}">@csrf @method('PATCH')<div><label class="muted">Status</label><select name="status"><option value="pendente" @selected($user->status->value==='pendente')>Pendente</option><option value="aprovado" @selected($user->status->value==='aprovado')>Aprovado</option><option value="bloqueado" @selected($user->status->value==='bloqueado')>Bloqueado</option></select></div><button class="btn secondary">Atualizar</button></form><form method="POST" action="{{ route('admin.users.products',$user) }}">@csrf @method('PUT')<div><label class="muted">Produtos liberados</label><select name="products[]" multiple>@foreach($products as $product)<option value="{{ $product->id }}" @selected($user->products->contains($product))>{{ $product->name }}</option>@endforeach</select></div><button class="btn">Salvar produtos</button></form></div></article>@endforeach</section></x-app-layout>
