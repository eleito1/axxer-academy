<x-app-layout title="Produtos — Administração"><div class="actions" style="justify-content:space-between"><div><h1>Produtos</h1><p>Gerencie os produtos e sua estrutura acadêmica.</p></div><a class="btn" href="{{ route('admin.products.create') }}">Novo produto</a></div><section class="card">@forelse($products as $product)<article class="user"><strong>{{ $product->name }}</strong> <span class="badge {{ $product->is_active?'aprovado':'bloqueado' }}">{{ $product->is_active?'ATIVO':'INATIVO' }}</span><p class="muted">{{ $product->courses_count }} cursos</p><div class="actions"><a class="btn" href="{{ route('admin.products.courses.index',$product) }}">Cursos</a><a class="btn secondary" href="{{ route('admin.products.edit',$product) }}">Editar</a><form method="POST" action="{{ route('admin.products.destroy',$product) }}" onsubmit="return confirm('Excluir produto?')">@csrf @method('DELETE')<button class="btn danger">Excluir</button></form></div></article>@empty<p>Nenhum produto cadastrado.</p>@endforelse</section></x-app-layout>
