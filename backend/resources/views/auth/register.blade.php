<x-app-layout title="Cadastro — AXXER Academy"><section class="card auth"><h1>Solicitar acesso</h1><p>Após o cadastro, um administrador analisará sua solicitação.</p>@if($errors->any())<div class="errors"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="POST" action="{{ route('register.store') }}">@csrf<div class="grid"><div class="field"><label>Nome</label><input name="name" value="{{ old('name') }}" required></div><div class="field"><label>Empresa</label><input name="company" value="{{ old('company') }}" required></div><div class="field"><label>Cidade</label><input name="city" value="{{ old('city') }}" required></div><div class="field"><label>WhatsApp</label><input name="whatsapp" value="{{ old('whatsapp') }}" required></div></div><div class="field"><label>Produto de interesse</label><select name="interested_product_id"><option value="">Selecione</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(old('interested_product_id') == $product->id)>{{ $product->name }}</option>@endforeach</select></div><div class="field"><label>E-mail</label><input type="email" name="email" value="{{ old('email') }}" required></div><div class="grid"><div class="field"><label>Senha</label><input type="password" name="password" required></div><div class="field"><label>Confirmar senha</label><input type="password" name="password_confirmation" required></div></div><button class="btn">Enviar solicitação</button></form><div class="links"><a href="{{ route('login') }}">Voltar para o login</a></div></section></x-app-layout>
