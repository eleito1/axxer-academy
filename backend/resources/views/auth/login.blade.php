<x-app-layout title="Entrar — AXXER Academy"><section class="card auth"><h1>Entrar</h1><p>Acesse os treinamentos dos produtos liberados para sua empresa.</p>@if($errors->any())<div class="errors">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.store') }}">@csrf<div class="field"><label for="email">E-mail</label><input id="email" type="email" name="email" value="{{ old('email') }}" required autofocus></div><div class="field"><label for="password">Senha</label><input id="password" type="password" name="password" required></div><label><input type="checkbox" name="remember" value="1"> Manter conectado</label><div style="margin-top:18px"><button class="btn">Entrar</button></div></form><div class="links">Ainda não possui acesso? <a href="{{ route('register') }}">Solicitar cadastro</a></div></section></x-app-layout>
