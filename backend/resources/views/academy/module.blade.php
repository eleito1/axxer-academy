<x-app-layout title="{{ $module->title }} — AXXER Academy"><a href="{{ route('academy.courses.show',[$product,$course]) }}">← {{ $course->title }}</a><h1>{{ $module->title }}</h1><p>{{ $module->description }}</p><section class="card">@forelse($module->lessons as $lesson)<div class="user"><strong>{{ $lesson->title }}</strong><p>{{ $lesson->description }}</p><a class="btn" href="{{ route('academy.lessons.show',[$product,$course,$module,$lesson]) }}">Assistir aula</a></div>@empty<p>Nenhuma aula publicada.</p>@endforelse</section></x-app-layout>
