<x-app-layout title="{{ $course->title }} — AXXER Academy"><a href="{{ route('academy.products.show',$product) }}">← {{ $product->name }}</a><h1>{{ $course->title }}</h1><p>{{ $course->description }}</p>@forelse($course->modules as $module)<section class="card" style="margin-top:18px"><h2>#{{ $module->order }} · {{ $module->title }}</h2><p>{{ $module->description }}</p>@foreach($module->lessons as $lesson)<div class="user"><a href="{{ route('academy.lessons.show',[$product,$course,$module,$lesson]) }}"><strong>{{ $lesson->title }}</strong></a><span class="muted"> · {{ $lesson->duration?gmdate('i:s',$lesson->duration):'--:--' }}</span></div>@endforeach<a class="btn secondary" href="{{ route('academy.modules.show',[$product,$course,$module]) }}">Ver módulo</a></section>@empty<p>Nenhum módulo disponível.</p>@endforelse</x-app-layout>
