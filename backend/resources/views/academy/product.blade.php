<x-app-layout title="{{ $product->name }} — AXXER Academy"><a href="{{ route('dashboard') }}">← Meus produtos</a><h1>{{ $product->name }}</h1><p>{{ $product->description }}</p><div class="grid">@forelse($courses as $item)<article class="card">@if($item['course']->cover_image)<img src="{{ $item['course']->cover_image }}" alt="" style="width:100%;height:160px;object-fit:cover;border-radius:8px">@endif<h2>{{ $item['course']->title }}</h2><p>{{ $item['course']->description }}</p><strong>{{ $item['percentage'] }}%</strong><div style="height:7px;background:#edf0f5;border-radius:10px;margin:8px 0 18px"><div style="height:100%;width:{{ $item['percentage'] }}%;background:#1559e8;border-radius:10px"></div></div>@if($item['certificate_available'])<p><span class="badge aprovado">✓ Certificado disponível</span></p>@endif<a class="btn" href="{{ route('academy.courses.show',[$product,$item['course']]) }}">{{ $item['percentage']>0?'Continuar':'Começar curso' }}</a></article>@empty<p>Nenhum curso publicado.</p>@endforelse</div></x-app-layout>
